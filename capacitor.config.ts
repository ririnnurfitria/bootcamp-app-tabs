import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.sch.smkn6malang.bootcamp',
  appName: 'bootcamp-app-tabs',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
